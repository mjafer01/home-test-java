import React from "react";
import DashboardProps from "../types/DashboardProps";

const Dashboard: React.FC<DashboardProps> = ({ urlProps, testID }) => {
  return <>dashboard</>;
};
export default Dashboard;
